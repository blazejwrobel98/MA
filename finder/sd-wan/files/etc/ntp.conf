# auto-generated from edge device configuration

restrict -4 default nomodify nopeer noquery notrap
restrict -6 default nomodify nopeer noquery notrap

restrict 127.0.0.1
restrict ::1

driftfile  /etc/ntp/ntp.drift

server 0.pool.ntp.org iburst minpoll 8
server 1.pool.ntp.org iburst minpoll 8
server 2.pool.ntp.org iburst minpoll 8
server 3.pool.ntp.org iburst minpoll 8

# additional declarations as needed
